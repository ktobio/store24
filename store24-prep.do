/**********************************************************************************File        : store24-prep.do  <--- this should be the exact name of THIS documentAuthor      : Kristina Tobio Created     : 03 Feb 2017Modified    : 03 Feb 2017Description : Sets up data for parts A and B**********************************************************************************/// these commands prepare your computer for the data and analysis// this finds and closes open log filescapture log close        // this clears Stata's data memory so new data can be loadedclear       // this makes it so you don't have to keep pressing return/enter to scroll through resultsset more off          // this sets memory sizeset mem 100m       // this keeps everything visible on a normal monitor or laptop screenset linesize 200    // cd means "change directory"// you need to change this to the location on your computer where you are storing the .do and data files*cd "/Users/ktobio/Desktop/Jeff/Course/Store24/store24-case/"       // this creates a log file, which will record all of the commands and outputs from this .do file // log files should be placed in a folder named "logs" in your directory log using "logs/store24-case-a", replace// data should be placed in a folder named "data" in your directory // the data is in .csv (comma seperated values) format// this command reads the data into Statainsheet using "data/store24-case-data.csv", names// code here that labels the variables?// save the data as a Stata datasetsave "data/store24-case-data.dta", replace// creating part A datasetkeep store sales profit mtenure ctenure pop comp visibility pedcount res hours24label var store "Store number"label var sales "FY 2000 sales"label var profit "FY 2000 profit before corp. overhead alloc, rent, deprec"label var mtenure "Avg manager tenure FY 2000"label var ctenure "Avg crew tenure FY 2000"label var pop "Pop w/in 1/2 mile radius"label var comp "# Competitors w/in 1/2 mile radius"label var visibility "5 pt rating visibility storefront, 5=highest"label var pedcount "5 pt rating foot traffic, 5=highest"label var res "Indicator for location in residental/urban area, 1=res"label var hours24 "Indicator for open 24 hrs, 1=open"save "data/PA_Store24A_data.dta", replacedrop _alluse "data/store24-case-data.dta"label var store "Store number"label var sales "FY 2000 sales"label var profit "FY 2000 profit before corp. overhead alloc, rent, deprec"label var mtenure "Avg manager tenure FY 2000"label var ctenure "Avg crew tenure FY 2000"label var pop "Pop w/in 1/2 mile radius"label var comp "# Competitors w/in 1/2 mile radius"label var visibility "5 pt rating visibility storefront, 5=highest"label var pedcount "5 pt rating foot traffic, 5=highest"label var res "Indicator for location in residental/urban area, 1=res"label var hours24 "Indicator for open 24 hrs, 1=open"label var servqual "Avg service quality rating FY 2000"label var mgrskill "Avg mgr skill rating FY 2000"label var crewskill "Avg crew skill rating FY 2000"save "data/PA_Store24B_data.dta", replace// closes your loglog close// drops all data from Stata's memoryclear